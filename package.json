{
  "name": "lanmap",
  "version": "0.2.0",
  "private": true,
  "description": "See what's on your network.",
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build -w @lanmap/shared && npm run build -w lanmap-backend && npm run build -w lanmap-frontend",
    "build:win": "node scripts/build-windows.mjs",
    "build:win:launcher": "go build -o dist/lanmap.exe ./windows/launcher.go",
    "postinstall": "npm run build -w @lanmap/shared",
    "test": "npm run test --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  },
  "engines": {
    "node": ">=22"
  }
}
