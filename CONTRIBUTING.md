# Contributing

Node 22+, npm. `npm install` at the repo root.

- Backend: `npm run dev -w lanmap-backend`
- Frontend: `npm run dev -w lanmap-frontend`
- Tests: `npm test --workspaces`
- Keep deps minimal; no ORM, no UI framework. Hand CSS.
- Only scan networks you own. Scan targets are validated server-side.
